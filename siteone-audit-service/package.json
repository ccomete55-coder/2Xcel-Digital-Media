{
  "name": "siteone-audit-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP wrapper around the SiteOne Crawler binary so it can be triggered and polled programmatically (Render web service). Bare CLI tools can't be a Render Web Service on their own — this gives it a health-checked port and a job API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
